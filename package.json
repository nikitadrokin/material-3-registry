{
  "name": "material-3-registry",
  "private": true,
  "workspaces": [
    "apps/*",
    "md3-registry"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=showcase",
    "build": "npm run build --workspace=showcase",
    "preview": "npm run preview --workspace=showcase"
  }
}
